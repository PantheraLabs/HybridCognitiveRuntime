{
  "name": "hcr-resume",
  "displayName": "HCR Resume",
  "description": "Resume development sessions without re-explaining context",
  "version": "0.1.0",
  "publisher": "pantheralabs",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/PantheraLabs/HybridCognitiveRuntime"
  },
  "homepage": "https://github.com/PantheraLabs/HybridCognitiveRuntime#readme",
  "bugs": {
    "url": "https://github.com/PantheraLabs/HybridCognitiveRuntime/issues"
  },
  "license": "SEE LICENSE IN ../../LICENSE",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Machine Learning",
    "Other"
  ],
  "keywords": [
    "cognitive",
    "state",
    "context",
    "resume",
    "ai"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "hcr.resume",
        "title": "HCR: Resume Session",
        "category": "HCR"
      },
      {
        "command": "hcr.showState",
        "title": "HCR: Show Current State",
        "category": "HCR"
      },
      {
        "command": "hcr.clearState",
        "title": "HCR: Clear Session State",
        "category": "HCR"
      },
      {
        "command": "hcr.startServer",
        "title": "HCR: Start Engine Server",
        "category": "HCR"
      }
    ],
    "configuration": {
      "title": "HCR Resume",
      "properties": {
        "hcr.autoResume": {
          "type": "boolean",
          "default": true,
          "description": "Automatically resume session on window focus after idle time"
        },
        "hcr.idleThreshold": {
          "type": "number",
          "default": 30,
          "description": "Minutes of idle time before triggering auto-resume"
        },
        "hcr.outputFormat": {
          "type": "string",
          "enum": ["text", "json"],
          "default": "text",
          "description": "Output format for session resume"
        },
        "hcr.commandPath": {
          "type": "string",
          "default": "",
          "description": "Optional path to the installed hcr CLI executable. If empty, the extension uses hcr.pythonPath or python."
        },
        "hcr.pythonPath": {
          "type": "string",
          "default": "",
          "description": "Optional Python executable used to launch the local HCR server when hcr.commandPath is not set."
        },
        "hcr.serverPort": {
          "type": "number",
          "default": 8733,
          "description": "Local HTTP port for the HCR engine server."
        }
      }
    },
    "views": {
      "explorer": [
        {
          "id": "hcr.sessionView",
          "name": "HCR Session",
          "when": "hcr.hasState"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "16.x",
    "typescript": "^4.9.4"
  }
}
